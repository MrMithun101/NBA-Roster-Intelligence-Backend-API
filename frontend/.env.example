# API base URL. Vite requires VITE_ prefix for env vars exposed to client.
#
# Local dev with Vite proxy (recommended):
#   VITE_API_BASE_URL=/api
#
# Direct to backend (no proxy):
#   VITE_API_BASE_URL=http://localhost:8000
#
# If unset, defaults to /api (works with vite.config.ts proxy).
VITE_API_BASE_URL=/api
