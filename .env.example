# Copy to .env and set real values. .env is gitignored so secrets are not committed.

# ---- Local dev (uvicorn, alembic, seed script) ----
# DATABASE_URL=postgresql://user:password@localhost:5432/nba_roster_db

# ---- Docker Compose (required for docker compose up) ----
POSTGRES_USER=app
POSTGRES_PASSWORD=
POSTGRES_DB=nba_roster_db

# ---- Optional: Redis ----
# REDIS_URL=redis://localhost:6379/0
