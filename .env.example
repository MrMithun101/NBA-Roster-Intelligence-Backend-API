# Database connection string
# Format: postgresql://username:password@host:port/database_name
# 
# Example for local PostgreSQL:
# DATABASE_URL=postgresql://postgres:password@localhost:5432/nba_roster_db
#
# Copy this file to .env and fill in your actual database credentials
# The .env file is ignored by git (it's in .gitignore) so your secrets stay safe

DATABASE_URL=postgresql://username:password@localhost:5432/nba_roster_db

# Optional: Redis for caching (teams list, roster). If unset or Redis is down, API skips cache.
# REDIS_URL=redis://localhost:6379/0
